const THREE = require('three');

var settings = {
	position: THREE.Vector2(0.0, 0.0),
	velocity: 0.0,
	goal: THREE.Vector2(0.0, 0.0),
	orientation: 0.0, //angle of orientation of agent with the forward z direction
	size: 1.0,
	markers: []
}
